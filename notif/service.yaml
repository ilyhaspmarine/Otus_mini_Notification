apiVersion: v1
kind: Service
metadata:
  name: {{ include "notif-app.serviceName" . | quote }}
  labels:
    app: {{ include "notif-app.appName" . | quote }}
spec:
  selector:
    app: {{ include "notif-app.appName" . | quote }}
  ports:
    - name: {{ .Values.service.portName }}
      protocol: TCP
      port: {{ int .Values.service.port }}
      targetPort: 8000
  type: {{ .Values.service.type }}
